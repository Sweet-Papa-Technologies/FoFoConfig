# Nginx directive reference (lazy-loaded)

This file is loaded on demand via `skill_view("nginx-config", "references/nginx-directives.md")` when the operator asks about a specific directive not covered in the main SKILL.md. Populated incrementally as FoFoConfig encounters new directives.

(Empty for v0.1; FoFoConfig may append entries here via `skill_manage write_file` over time.)
